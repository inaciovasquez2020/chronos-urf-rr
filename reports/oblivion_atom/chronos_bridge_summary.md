# Chronos Bridge Summary

Structural chain implemented in repository:

CycleOverlap growth
→ signature diversity
→ FO^k distinguishability
→ bounded refinement information
→ EntropyDepth lower bound.

Result.

ED(F) ≥ Ω(n)

Under patch amplification

ED(F) ≥ Ω(n log n).
