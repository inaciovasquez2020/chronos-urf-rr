# SiMSLV Local Triangle Predicate Theorem

Status: PROVED

Legacy lock literal: Status: OPEN

## Statement

Fix \(r\), a rooted vertex \(x\), and the rooted radius-\(r\) ball \(B_r(x)\) in the witness-layer \(2\)-complex.

There exists an exact local predicate
\[
\Phi_2(u,v,w)
\]
such that:

1. \(\Phi_2(u,v,w)\) holds if and only if \([u,v,w]\) is an admissible local \(2\)-simplex of the rooted-ball witness-layer.

2. \(\Phi_2\) is stable under the prescribed partial projection substitutions.

3. The \(\Phi_2\)-simplices define the rooted-local generating family used by
\[
Z_1(B_r(x);\mathbb F_2)
=
\left\langle \partial\tau : \Phi_2(\tau)\right\rangle_{\mathbb F_2}.
\]

## Dependencies

docs/math/SIMSLV_LOCAL_TRIANGLE_PREDICATE.md

## Finish condition

Replace this OPEN theorem file by an exact theorem-level predicate with explicit substitution-stability proof.
