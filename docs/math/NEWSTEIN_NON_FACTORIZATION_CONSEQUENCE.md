# Newstein Non-Factorization Consequence

Status: OPEN

## Statement
From
\[
\operatorname{Type}_{k,r}(G_n)=\operatorname{Type}_{k,r}(H_n)
\quad\text{and}\quad
Q(G_n)\neq Q(H_n),
\]
deduce that \(Q\) does not factor through rooted radius-\(r\) type or \(FO^k_r\)-type.

## Required inputs
1. Rooted-ball trivialization theorem.
2. Quotient-gap theorem.
3. Definition of the quotient invariant \(Q\).
4. Factorization contradiction argument.

## Role
This locks item IV.2 in `docs/math/NEWSTEIN_REMAINING_THEOREM_LEVEL_OBLIGATIONS.md`.

## Finish condition
Replace this file by a proof sufficient to close the non-factorization branch.
