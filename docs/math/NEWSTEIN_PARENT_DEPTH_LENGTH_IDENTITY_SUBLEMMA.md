# NEWSTEIN_PARENT_DEPTH_LENGTH_IDENTITY_SUBLEMMA

Status: OPEN

## Statement

Let (B_R(r)) be a rooted ball with parent map (p) defined on (B_R(r)\setminus{r}).
The next missing object is the parent-depth-length identity:

[
\forall v\in B_R(r)\setminus{r},\quad
d\bigl(r,p(v)\bigr)=d(r,v)-1.
]

## Role in the closure chain

This is the next micro-step after the parent map definition.
It is the exact decrement law needed to certify that parent iteration strictly descends toward the root, which supports the rooted-tree certificate, local acyclicity, and the local coboundary criterion.
