# NEWSTEIN FIBER-QUOTIENT-RANK THEOREM

Status: OPEN

## Statement
Assume the theorem-level rooted-ball trivialization result in the Newstein quotient-gap regime.

Then the associated fiber quotient has the canonical rank required by the quotient-gap chain.

## Dependency Chain
ParentDepthDecrement^thm => RootedBallTrivialization^thm => FiberQuotientRank^thm => DirectSumIndependence^thm => PerStepInformationBound^thm => QuotientGapClosure^unconditional
