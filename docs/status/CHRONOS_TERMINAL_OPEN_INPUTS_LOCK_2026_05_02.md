# Chronos Terminal Open Inputs Lock — 2026-05-02

Status: Conditional / Frontier Formalization

## Terminal theorem-level inputs

Chronos theorem-level closure is blocked exactly by the following open mathematical inputs:

1. R1 — Long-Chord Exclusion Lemma
2. R2 — Diameter-Separation Filling Obstruction
3. R3 — Uniform Local-Type Capacity Lemma

## Dependency form

- R1 implies `dim(W^tw / W^triv) = 2`.
- R2 implies cross-fiber injectivity.
- R2 + R1 + sigma-package assembly imply `dim(Z₁ / W^glob) ≥ 2|U|`.
- R3 + `dim(Z₁ / W^glob) ≥ 2|U|` + `|U| → ∞` imply non-factorization.

## Boundary

No theorem-level closure is claimed here.

Build success, CI success, dashboards, ledgers, certificates, finite patches, axioms, admits, `sorry`, or placeholder witnesses do not constitute theorem-level closure.

No further theorem-level closure is possible without new mathematical input on R1, R2, and R3.
