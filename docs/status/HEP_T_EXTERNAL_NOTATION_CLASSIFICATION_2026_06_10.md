# HEP t External Notation Classification

Closed object:

`HEP_T_EXTERNAL_NOTATION_CLASSIFICATION_2026_06_10`

Classification:

`t_HEP ∈ ExternalDomainNotation`

`t_HEP ∉ CoreUnificationPrimitive`

`t_HEP ∉ CoreTheoremTarget`

Admissible scope:

- `t := top quark` only as typed external notation.
- `t := Mandelstam momentum-transfer variable` only as typed external notation.
- The unifying object is `(symbol, domain, type, admissibility_conditions, source_payload)`.

Boundary:

No top-quark theorem, no scattering-amplitude theorem, no QFT phenomenology expansion, and no Mandelstam-variable analytic closure are introduced.
