# Remaining irreducible gap

Corrected object:
bipartiteColumnCycle(A, γ) := every column is hit an even number of times by γ.

Closed reductions:
bipartiteColumnCycle(A, γ) -> evenIncidence(A, γ)
evenIncidence(A, γ) -> OverlapCycleCondition(A, γ)

Remaining open point:
construct γ from the intended overlap/incidence geometry so that bipartiteColumnCycle(A, γ) holds.
