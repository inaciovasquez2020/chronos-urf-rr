# Cyclone Pair-Type TODO

## Corrected target

Prove the Cyclone bound under pair-type equality rather than per-vertex type equality.

## Lean tasks

1. Define radius-r graph balls.
2. Define FO^k pair-types for ordered adjacent pairs.
3. Encode the cycle predicate as an edge relation.
4. Define symmetric difference D = C1 ⊕ C2.
5. Define connected components of D.
6. Prove each component is an alternating cycle.
7. Count pair-type labels.
8. Derive the bound |E(H)| ≤ L(k,Δ,R).

## Documentation tasks

1. update CLR dependency map
2. mark original per-vertex Cyclone statement as false
3. mark pair-type Cyclone statement as corrected target
