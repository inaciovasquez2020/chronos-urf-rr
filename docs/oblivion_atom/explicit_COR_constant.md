# Explicit COR Bound

From the local-cycle span bound,

COR_R(G) ≤ C(k,Δ,R)

with explicit constant

C(k,Δ,R) = N(k,Δ,R) · O(Δ^{2R})

where

N(k,Δ,R)

is the number of FO^k_R types.

Thus the bound is finite and depends only on k, Δ, and R.
