# Cycle Transport Rigidity

A graph G satisfies L(k,R) if:

For every cycle γ and every FO^k-type-preserving local transport φ,
the induced action satisfies:

[φ · γ] - [γ] ∈ Z1^{≤2R+1}(G)

Equivalent formulation:
Local transports act trivially on the quotient cycle space.
